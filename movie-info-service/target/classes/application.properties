server.port=8082

# Currently eureka server is running in the same machine and on the default port so we don't have to tell the client about
# the location of eureka server. Else we will have to.
spring.application.name=movie-info-service

api.key=07f5a99ea7f203ca58ef785015ca7895